# Архив Изменений Trekker Platform

### ✅ Выполнено (Сессия 28.01.2026)
- [x] Инициализация проекта (Monorepo: Core + Desktop)
- [x] Настройка сборки (Vite, Electron-Vite)
- [x] Настройка Базы Данных (Prisma + SQLite)
- [x] Миграция БД: Создание таблицы `Note`
- [x] Базовый UI: Компонент `NoteNode` для холста

### ✅ Выполнено (Сессия 29.01.2026)
- [x] Настроить IPC-каналы в Electron для работы с заметками (createNote, getNotes)
- [x] Реализовать обработчики IPC в основном процессе Electron с использованием Prisma
- [x] Интегрировать вызовы API в React-приложение и проверить сохранение в SQLite
- [x] Улучшена загрузка виджетов: теперь ноды заметок на холсте отображают реальное содержимое из БД.
- [x] Проведена очистка типов (electron.d.ts) и унификация доступа к API через `window.electron`.
- [x] **Сборка Production:** Настроен `electron-builder` с корректным копированием Prisma Engine и Schema. Создан рабочий `.exe` файл.
- [x] **Исправление Сборки (Build Fixes):**
    - Исправлена ошибка `Cannot find module`: библиотеки `@electron-toolkit` теперь бандлятся.
    - Исправлен "Белый экран": в Vite добавлен `base: './'`.
    - Исправлен роутинг: `BrowserRouter` заменен на `HashRouter` для поддержки файловой системы Electron.
    - Исправлен путь сборки рендерера: `dist/renderer` -> `out/renderer`.
- [x] **Инициализация БД:** Реализован механизм "Первого запуска" — копирование чистого шаблона `trekker.db` из ресурсов в `AppData`. Добавлена защита от коррупции базы.
- [x] **Отладка:** Добавлено файловое логирование (`main.log`) и авто-открытие DevTools для диагностики.
- [x] **Календарь:** Подключена библиотека `react-big-calendar`.
- [x] **БД (Календарь):** Создана модель `Event`, настроена миграция и IPC для сохранения событий.

### ✅ Выполнено (Сессия 30.01.2026)
- [x] **Улучшение UI Холста:**
    - Заметки сделаны непрозрачными (цвет по умолчанию `#fff9c4`) для лучшей читаемости.
    - Фон холста изменен на ненасыщенный индиго (`#eef2ff`), цвет точек сетки изменен на черный.
- [x] **Фикс Курсора:** Исправлен баг с исчезающим/белым курсором путем отключения аппаратного ускорения (`app.disableHardwareAcceleration`).
- [x] **Функционал Заметок (NotesPage):**
    - Вкладка "Diary" переименована в "Notes".
    - Полный рефакторинг UI: переход от списка к сетке стикеров (Grid Layout).
    - Реализована авто-нумерация списков (при вводе "1. " + Enter создается "2. ").
    - Редактирование через модальный оверлей.
- [x] **Архитектура (Global Widget Bar):**
    - Внедрена концепция глобального тулбара в `MainLayout`.
    - Реализован `ToolbarActionContext` для управления действиями тулбара (например, создание заметки) в зависимости от активной страницы.
- [x] **Календарь (Major Update):**
    - Реализован вид "Год" (Year View).
    - Упрощена навигация: удалены режимы "Неделя" и "День".
    - Интеграция с глобальным тулбаром для создания контекстных заметок.
- [x] **Интерактивность на Холсте (CanvasPage):**
    - Реализовано меню инструментов: "Курсор" (Выделение) и "Рука" (Перемещение).
    - Настроена логика ЛКМ/СКМ для разных режимов.
    - Включено прилипание к сетке (Snapping 10px).
    - Реализована логика "Bring to Front" (поднятие наверх) при перетаскивании.
- [x] **Конфигурация отладки:** По требованию пользователя DevTools оставлен включенным для дальнейшего тестирования.

### ✅ Выполнено (Сессия 04.02.2026)
- [x] **Удаление заметок с холста:**
    - Backend: Добавлен IPC метод `deleteWidget` для удаления виджетов из БД.
    - Frontend: В `NoteNode` добавлена кнопка удаления (иконка корзины), появляющаяся при наведении.
    - Logic: Реализовано обновление состояния `CanvasPage` после удаления.

### ✅ Выполнено (Сессия 10.02.2026)
- [x] **Унификация Drag-and-Drop (dnd-kit):**
    - Внедрена библиотека `@dnd-kit`. Создан `DndContext` в `App.tsx`.
    - Реализовано перетаскивание виджетов из тулбара на Холст и в Календарь.
    - Добавлен `DragOverlay` для отображения "призрака" виджета при перетаскивании.
- [x] **Холст (Major Update):**
    - Реализовано сохранение положения камеры (zoom/pan) в БД.
    - Добавлены 4 точки подключения (Handles) для заметок (сверху, снизу, слева, справа).
    - Реализовано сохранение связей (Lines/Edges) между заметками в БД.
    - Реализовано удаление связей двойным кликом.
    - Добавлен новый виджет `EventNode` (карточка события на холсте).
- [x] **Календарь (Major Update):**
    - **Вид "День" (`DayView`):** Полноценная страница дня со списком событий.
    - **Вид "Год" (`YearView`):** Добавлена визуализация диапазонов событий (цветная заливка дней).
    - **События:** Добавлено поле `color` в БД. Реализована кастомизация цвета событий.
    - **Редактирование:** Унифицировано модальное окно (`EventModal`) для создания/редактирования/удаления событий.
    - **Заметки:** Переработана панель контекстных заметок (`ContextNotesPanel`). Теперь она показывает все заметки за выбранный год, сгруппированные по категориям.
- [x] **UX/UI:**
    - Убрана навязчивая "автопрокрутка" средней кнопкой мыши.
    - Убрана прокрутка всего окна (теперь скроллится только контент внутри панелей).
    - Улучшены хитбоксы точек подключения на холсте.
