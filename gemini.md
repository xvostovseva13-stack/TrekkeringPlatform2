# Trekker Platform - Документация Проекта

Этот документ описывает архитектуру, структуру папок, инструменты и общие подходы к разработке приложения "Trekker Platform".

## 1. Философия и Подход

**Trekker Platform** — это мультиплатформенное приложение для персонального трекинга (привычки, финансы, цели, календарь, дневник) с интегрированным ИИ-ассистентом.

- **Единая кодовая база**: Мы используем React для создания основного интерфейса, который будет работать как в десктопной версии, так и в вебе.
- **Мультиплатформенность**: Electron используется для "обертки" React-приложения в нативные исполняемые файлы для Windows и macOS. Веб-версия будет доступна через браузер.
- **Монорепозиторий**: Проект организован как монорепозиторий с использованием `npm workspaces`. Это упрощает управление общим кодом и зависимостями между разными частями приложения (ядро, десктоп, сервер).
- **Интерфейс**: Дизайн в стиле "Чистый SaaS" — современный, легкий и отзывчивый. Используется карточная система и концепция "Бесконечного холста" (Infinite Canvas). Пользователь управляет виджетами (графики, заметки, трекеры), которые можно свободно размещать на дашбордах. Визуальный стиль: мягкие тени, скругленные углы и много свободного пространства ("воздуха"). Основной акцент на UX и читаемости данных.
- **Очистка данных**: В фазе активного тестирования мы поддерживаем чистоту базы данных, удаляя артефакты от старых фич (например, заметки для "недель" и "дней" после их удаления из календаря), чтобы избежать багов и путаницы.

## 2. Инструменты и Библиотеки

- **Язык**: TypeScript
- **Монорепозиторий**: npm Workspaces
- **Форматирование кода**: Prettier
- **Линтинг**: ESLint

### Фронтенд (Ядро приложения)
- **Фреймворк**: React
- **Сборщик**: Vite
- **Стилизация**: Bootstrap, React-Bootstrap, CSS Modules/SASS
- **Иконки**: `react-icons`
- **Графики**: `recharts`
- **Работа с датами**: `dayjs`
- **Роутинг**: `react-router-dom`
- **Календарь**: `react-big-calendar`
- **Таблицы**: В процессе выбора (планируется `ag-grid-react` или `@tanstack/react-table`)

### Десктоп-версия (Local First)
- **Фреймворк**: Electron
- **Сборщик**: `electron-vite`
- **БД (Локальная)**: SQLite + Prisma (хранение данных на устройстве пользователя)

### Бэкенд (Синхронизация - Планируется)
- **Среда выполнения**: Node.js
- **Фреймворк**: Express.js
- **Работа с БД**: Prisma (PostgreSQL для синхронизации)

## 3. Структура Папок

Проект будет иметь следующую структуру:

```
/
├── gemini.md               # Этот файл
├── package.json            # Корневой package.json для управления воркспейсами
├── packages/               # Директория для фронтенд-пакетов
│   ├── core/               # Основное React-приложение (UI и логика)
│   │   ├── public/         # Статические ассеты (иконки, шрифты)
│   │   ├── src/
│   │   │   ├── assets/         # Шрифты, иконки, изображения
│   │   │   ├── components/     # Общие переиспользуемые компоненты
│   │   │   │   ├── canvas/     # Компоненты для бесконечной доски (NoteNode и др.)
│   │   │   │   └── ...
│   │   │   ├── features/       # Логика и компоненты для каждой фичи
│   │   │   │   ├── ai-assistant/
│   │   │   │   ├── calendar/
│   │   │   │   ├── diary/
│   │   │   │   ├── finance/
│   │   │   │   ├── goals/
│   │   │   │   └── habits/
│   │   │   ├── layouts/        # Компоненты для построения макета (Sidebar, Topbar, MainLayout)
│   │   │   ├── pages/          # Страницы-контейнеры
│   │   │   │   ├── CanvasPage.tsx # Страница бесконечной доски
│   │   │   │   ├── NotesPage.tsx  # Страница заметок
│   │   │   │   └── ...
│   │   │   ├── routes/         # Настройка роутинга
│   │   │   ├── styles/         # Глобальные стили, переменные
│   │   │   ├── App.tsx         # Корневой компонент приложения
│   │   │   └── main.tsx        # Точка входа в React-приложение
│   │   ├── index.html      # Главный HTML-файл для Vite
│   │   ├── package.json
│   │   └── vite.config.ts
│   │
│   └── desktop/              # Electron-обертка + Локальная БД
│       ├── src/
│       │   ├── main.ts       # Главный процесс Electron
│       │   └── preload.ts    # Скрипт для связи главного процесса и рендерера
│       ├── prisma/           # Схема БД и миграции
│       │   ├── migrations/
│       │   └── schema.prisma
│       ├── trekker.db        # Локальная база данных SQLite
│       ├── electron.vite.config.ts
│       └── package.json
│
└── server/                   # Бэкенд-приложение (для будущей синхронизации)
    ├── src/
    │   ├── api/
    │   ├── config/
    │   ├── services/
    │   └── server.ts
    └── package.json
```

## 5. Архитектурная концепция: Global Widget Bar

Мы переходим от статической панели инструментов к концепции **"Global Widget Bar"**.

-   **Философия**: Toolbar — это не просто набор кнопок, а "док" или "библиотека" активных виджетов.
-   **Single Source of Truth**: Каждый виджет (Заметка, Список задач, Трекер привычки) имеет уникальный `UUID` и хранится в БД.
-   **Мульти-инстанцирование**: Один и тот же виджет может отображаться одновременно в нескольких местах:
    -   В Глобальном Баре (как иконка/мини-версия).
    -   На Холсте (как развернутая нода).
    -   В Сайдбаре (как элемент списка).
-   **Синхронизация**: Изменение виджета в любом месте (например, редактирование текста заметки на Холсте) мгновенно отражается во всех его экземплярах (в Баре, в Календаре и т.д.) благодаря подписке на UUID.
-   **Типы Виджетов**:
    -   Заметки (Notes)
    -   Списки задач (To-Do List) — *в планах*
    -   Трекеры привычек (Habit Tracker)
    -   Таймеры (Pomodoro) — *в планах*
    -   Мини-календари — *в планах*

## 🤖 AI Operational Rules (Правила поведения)

### Rule: Proactive Suggestions (Инициатива)
В конце КАЖДОГО ответа, связанного с написанием кода или архитектурой, ты должен добавить короткий блок:
**🚀 Идея для улучшения:**
* [Предложи одну конкретную идею, фичу или рефакторинг, связанный с тем кодом, который мы только что трогали].
* [Объясни в одном предложении, почему это сделает Trekker Platform круче].

Пример: Если мы правим календарь, предложи "Добавить Drag-and-Drop событий из внешних файлов .ics".

### Rule: Smart Session Wrap-up & Archiving (Умное завершение сессии)

Когда я пишу команду "Заверши сессию", "Wrap up" или "Конец работы", ты ОБЯЗАН выполнить этот алгоритм строго по шагам:

**Шаг 1: Актуализация**
1. Проанализируй изменения в коде за текущую беседу.
2. Перемести выполненные задачи в новый блок `### ✅ Выполнено (Сессия [Сегодняшняя Дата])` в разделе "История".
3. Обнови "План на следующую сессию", убрав сделанное и перенеся несделанное.

**Шаг 2: Гигиена контекста (Auto-Archiving)**
1. Посчитай количество блоков `### ✅ Выполнено` в файле `gemini.md`.
2. **ЕСЛИ блоков больше 3 (трех):**
   * Оставь в `gemini.md` только **3 самые свежие** сессии.
   * Все более старые сессии (текст и задачи) **вырежи** из этого файла.
   * **Допиши** (Append) эти старые сессии в файл `docs/changelog_archive.md` (создай файл и папку, если их нет).
   * В архивном файле сохраняй хронологический порядок.

**Шаг 3: Стратегия**
1. Предложи 2-3 идеи для *новых* фич (Proactive Suggestions), исходя из того, что мы только что закончили.
2. Выведи краткий итог: "Сессия закрыта. [Кол-во] старых записей перенесено в архив. План обновлен."

## 6. План разработки и Журнал

### ✅ Выполнено (Сессия 11.02.2026)
- [x] **Event Bus (Real-time Sync):**
    - Реализована утилита `eventBus.ts` для типизированных глобальных событий (`EVENT_CHANGED`, `NOTE_CHANGED`).
    - **CanvasPage:** Подписка на изменения событий. Автоматическая перезагрузка виджетов при изменении данных в других частях приложения (например, через модальное окно).
    - **CalendarPage:** Подписка на изменения событий и заметок. Синхронизация списка событий и контекстных заметок в реальном времени.
    - **Refactoring:** Очистка кода от неиспользуемых импортов и переменных для соответствия Strict TypeScript.
    - **Fixes:** Исправлены типы `EventListener` и добавлена конфигурация плагина `isBetween` для `dayjs`.

### ✅ Выполнено (Сессия 12.02.2026)
- [x] **Привычки (Habits 2.0):**
    - **Редызайн:** Обновлен UI карточек и сводки (стиль Clean SaaS, серии/streaks 🔥).
    - **Виджет для Холста:** Создан `HabitNode` — интерактивный мини-трекер с сеткой на 7 дней (вчера/сегодня/завтра) и прогресс-баром.
    - **Гибкий График:** Реализована настройка "Custom Schedule" (например, 2 через 2, 5 через 2).
    - **Визуализация:** Выходные дни в графике теперь отображаются явной штриховкой.
    - **Единое редактирование:** Унифицировано модальное окно `HabitModal` для использования на странице привычек и на холсте.
- [x] **Контекст (Used In):**
    - В карточки заметок и привычек добавлен индикатор "Active in: ...", показывающий, где используется элемент (Canvas, Calendar).
    - Бэкенд (`main.ts`): Обновлены `getNotes` и `getHabits` для сбора информации о связанных виджетах и контейнерах.
- [x] **Архитектура:**
    - Реализован IPC метод `db:updateWidget` для обновления настроек и привязок виджетов.
    - Подготовлен фундамент для миграции календаря на систему виджетов (`db:addWidgetToDate`).
    - Тулбар: Добавлен виджет "Habit" (Drag-and-Drop).

### ✅ Выполнено (Сессия 19.02.2026)
- [x] **Унификация редактирования (Заметки):**
    - Создан компонент `NoteModal` для стандартизации UI (аналогично Events/Habits).
    - В `NoteNode` восстановлена логика авто-списков (1. -> 2.) и сохранение по `Ctrl+Enter`.
    - Убрано инлайн-редактирование в пользу модального окна.
- [x] **Глобальный Тулбар (Контекстное меню):**
    - Реализован компонент `WidgetHistoryMenu` (всплывающее меню).
    - **UX:** ПКМ по иконке тулбара показывает 3 последних созданных элемента.
    - **API:** Добавлен метод `db:getRecentItems`.
    - **Drag-and-Drop:** Реализовано перетаскивание элементов "из истории" на холст (создание виджета-ссылки).
- [x] **Привычки (UX/UI):**
    - **Drag-and-Drop:** При перетаскивании новой привычки на холст сразу открывается модальное окно настройки.
    - **Статистика:** Исправлен расчет серий (Streaks). Выходные (Rest Days) теперь корректно "замораживают" серию, не сбрасывая её.
    - **Визуализация:** Добавлено отображение **Best Streak** (Рекорд 🏆) рядом с текущей серией (🔥).

### 📅 План на следующую сессию
- [ ] **Сайдбар (Библиотека Виджетов):** Реализовать вкладку "Library" со списком всех заметок и привычек для Drag-and-Drop на холст (повторное использование).
- [ ] **Мини-календарь:** Добавить виджет календаря для холста.
- [ ] **Поиск:** Внедрить поиск по виджетам в контекстное меню истории или глобальный поиск.

### 🔮 Бэклог (Будущие задачи)
- [ ] **Миграция Календаря:** Перевести ячейки календаря на систему контейнеров.
- [ ] Исследовать возможность добавления новых типов виджетов (например, "Список дел").
- [ ] Добавить к заметкам дату их создания.
